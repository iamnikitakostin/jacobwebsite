import React from 'react';
import Nav from './Nav';

const Header = () => {
  return (
    <header>
      <div className="bg-orange-500 text-white text-center py-2">
        Some recent updates here, or maybe a promocode to track the people coming from the website
      </div>
    </header>
  );
};

export default Header;
